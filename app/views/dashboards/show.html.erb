<br />

<div class="booyah-box col-10 offset-1">
  <center><h1>What I'm Learning</h1></center>
  <br />
  <% current_user.enrolled_courses.each do |course| %>
    <h3 class="text-center">
      <%= link_to course.title, course_path(course) %>
    </h3>
    <br />
    <%= image_tag course.image.url, class: 'img-fluid center' %>
    <br />
    <div class="row">
      <div class="col-12 col-sm-8 padding-left">
        <%= course.description %>
      </div>
      <div class="col-12 col-sm-4 text-center">
    <%= link_to "More Info...", course_path(course), class: 'btn btn-lg btn-primary' %>
      </div>
    </div>
    <hr class="col-8 offset-2">
    <br class="clr" />
  <% end %>

   <center><h1>What I'm Teaching</h1></center>
   <br />
    <% current_user.courses.each do |course| %>
      <h3 class="text-center">
        <%= link_to course.title, course_path(course) %>
      </h3>
      <br />  
      <%= image_tag course.image.url, class: 'img-fluid center' %>
      <br />
      <div class="row">
        <div class="col-12 col-sm-8 padding-left">
          <%= course.description %>
        </div>

        <div class="col-12 col-sm-4 text-center">
          <%= link_to "More Info...", course_path(course), class: 'btn btn-lg btn-primary' %>
        </div>
      </div>
      <hr class="col-8 offset-2">
      <br class="clr" />
    <% end %>
</div>
